# Standalone benchmarks — not wired into add_libc_unittest.
#
# These are manual build-and-run development benchmarks that link against
# ucrt/msvcrt and use int main(). They are not part of the ninja check-libc
# suite. See the build instructions at the top of each file.
